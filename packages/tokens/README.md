# @commonspace/tokens

Layered design tokens for Commonspace UI.

```ts
import { referenceColors, space } from '@commonspace/tokens';
import '@commonspace/tokens/styles.css';
```

The CSS file provides reference tokens, semantic roles, light/dark/system/high-contrast themes, accent variants, and compact density overrides.
