# @unpopping-candy/tokens

Layered design tokens for Unpopping Candy.

```ts
import { referenceColors, space } from '@unpopping-candy/tokens';
import '@unpopping-candy/tokens/styles.css';
```

The CSS file provides reference tokens, semantic roles, light/dark/system/high-contrast themes, accent variants, and compact density overrides.
