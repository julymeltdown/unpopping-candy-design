{
  "name": "@commonspace/evals",
  "version": "0.2.0",
  "description": "Deterministic evaluation harness for AI-generated Commonspace interfaces.",
  "license": "UNLICENSED",
  "type": "module",
  "files": ["dist", "fixtures", "README.md"],
  "sideEffects": false,
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "publishConfig": { "access": "public" },
  "dependencies": {
    "@commonspace/knowledge": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "24.3.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
