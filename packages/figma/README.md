# @unpopping-candy/figma

Generates versioned Figma Code Connect manifests and parserless `.figma.ts` templates from the canonical Unpopping Candy knowledge catalog.

The checked-in configuration intentionally starts with placeholder Figma URLs. `npm run figma:publish-check` fails until each component has a real Figma component node URL, preventing accidental publication of fake mappings.
